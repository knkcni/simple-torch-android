apply plugin: 'com.android.application'

android {
    compileSdkVersion 25
    buildToolsVersion '26.0.2'

    defaultConfig {
        applicationId "com.kippysavantures.simpletorch"
        minSdkVersion 16
        targetSdkVersion 19
        versionCode 1
        versionName "1.6"
    }
    buildTypes {
        release {

        }
    }
}

dependencies {
    compile fileTree(dir: 'libs', include: ['*.jar'])
}
