<FrameLayout
    android:layout_width="fill_parent"
    android:layout_height="fill_parent"
    android:background="#FF7F00"
    android:id="@+id/frameTorchLayout"
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:clickable="true">

    <!--<ToggleButton
        android:textSize="@dimen/taille_text_togglebutton"
        android:layout_width="200dp"
        android:layout_height="200dp"
        android:textOff="@string/off_flash"
        android:textOn="@string/on_flash"
        android:id="@+id/light_button"
        android:layout_gravity="center" />-->

    <!--<ImageButton
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:keepScreenOn="true"
        android:src="@drawable/visuel_on3"
        android:id="@+id/imageButton"
        android:layout_gravity="center"
        android:clickable="false"
        android:visibility="visible"
        android:contentDescription="@string/on_button" />-->

    <ImageView
        android:contentDescription="@string/on_button"
        android:layout_width="wrap_content"
        android:src="@drawable/visuel_on3"
        android:layout_height="wrap_content"
        android:id="@+id/torchView"
        android:layout_gravity="center" />
</FrameLayout>